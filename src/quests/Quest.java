package quests;

/**
 *
 * @author UndeadScythes
 */
public interface Quest {
    public enum Type {
        FETCH,
        COLLECT,
        RESCUE,
        CLEAR,
        ESCORT,
        ASSASINATE,
        EXPLORE;
    }
}
